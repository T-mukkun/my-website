# ウェブサイト運用リスク管理

- 報告者：Codex
- 報告日時：2026年05月29日 08:39:31 +09:00
- 対象フォルダ：`C:\Users\mukaihata\Dropbox\mukaihata\研究\webサイト\webサイト_研究\my-website`
- 調査方針：読み取りのみ。ファイルの変更・削除・移動は未実施。

## 調査結果

| 項目 | 状態 | 内容 | 推奨対応 |
|---|---|---|---|
| 1. 機密情報のチェック | ✅ 問題なし | 対象拡張子 `.html .js .json .env .md .txt .py .ts` のうち、実在したのは `index.html` と `.claude\launch.json`。APIキー、パスワード、トークン、シークレット系の直接記述は見つかりませんでした。 | なし |
| 2. `.git` フォルダ | ⚠️ 要注意 | `.git` は存在します。`.git\index.lock` などの `.lock` ファイルは見つかりませんでした。 | Dropbox同期下にあるため、下記3の対応を推奨します。 |
| 2. `.gitignore` | ⚠️ 要注意 | `.gitignore` は存在しません。したがって `.env` や機密ファイルの除外設定もありません。 | `.gitignore` を作成し、`.env`、`*.key`、`*.pem`、認証情報ファイルなどを除外対象にすることを推奨します。 |
| 3. Dropbox同期対象 | ❌ 問題あり | パスに `Dropbox` が含まれており、同期対象内と判断できます。さらに `.git` フォルダもその中にあります。 | `.git` をDropbox同期対象外の場所に置く、またはリポジトリ全体をDropbox外へ移すことを推奨します。同期競合でGit管理情報が壊れるリスクがあります。 |
| 4. GitHub公開リスク | ⚠️ 要注意 | `.git\config` のリモートは `https://T-mukkun@github.com/T-mukkun/my-website.git`。ただし `.git\config` だけでは Public / Private は判定できません。 | GitHub上でリポジトリの公開設定を確認してください。公開リポジトリの場合、画像・HTML・メールアドレス等が公開対象になります。 |
| 4. 依存ファイル | ✅ 問題なし | `package.json`、`composer.json`、`requirements.txt`、`pyproject.toml` などの依存ファイルは見つかりませんでした。 | なし |
| 5. 接続方式 | ⚠️ 要注意 | GitHubリモートURLは `https://` 方式です。URL内にパスワードやトークンは含まれていませんが、HTTPS認証方式です。 | 可能であればSSH方式 `git@github.com:...` への切り替えを検討してください。 |
| 5. `.netrc` / 認証情報 | ✅ 問題なし | プロジェクト直下、ユーザーホームの `.netrc` / `_netrc` は見つかりませんでした。平文認証情報ファイルは確認されませんでした。 | なし |
| 5. `credential.helper` | ⚠️ 要注意 | `.git\config` 内に `credential.helper` はありません。グローバル設定も取得できませんでした、または未設定でした。 | Git認証で毎回入力が必要な場合は、OS標準の安全な認証管理を使う設定を検討してください。 |
| 6. ファイル構成 | ⚠️ 要注意 | 直下には `.claude`、`.git`、`index.html`、複数の画像ファイルがあります。`.claude\launch.json` はローカルサーバー起動設定で、機密情報はありません。 | `.git` がWeb公開ルートやDropbox同期内にある点に注意してください。 |

## 特に気になる点

| 状態 | 内容 | 推奨対応 |
|---|---|---|
| ❌ 問題あり | `.git` がDropbox同期対象内にあります。 | リポジトリをDropbox外へ移すことを推奨します。 |
| ⚠️ 要注意 | `.gitignore` がありません。 | `.env` や秘密鍵などを除外する `.gitignore` を用意してください。 |
| ⚠️ 要注意 | GitHubリモートはHTTPS方式です。 | SSH方式への切り替えを検討してください。 |


## 追加報告（報告者：Antigravity）

- 報告者：Antigravity
- 報告日時：2026年05月29日 08:42:00 +09:00

先の調査結果に加え、以下の追加確認および詳細な状況の把握を行いました。

### 追加調査結果

| 項目 | 状態 | 内容 | 推奨対応 |
|---|---|---|---|
| 4. GitHub公開リスク (Public/Private確定) | ⚠️ 要注意 | GitHub上のリポジトリ `T-mukkun/my-website` に外部からアクセス可能であり、**Public（公開リポジトリ）** であることを確認しました。 | 静的ポートフォリオサイトとしては公開で問題ありませんが、今後の開発で機密性の高いファイルを追加しないよう、常に意識する必要があります。 |
| 5. `credential.helper` の設定 | ✅ 問題なし | システム環境を調査した結果、`credential.helper` にはセキュアなOS標準の **`manager` (Git Credential Manager)** が設定されていることを確認しました。平文での保存はありません。 | 特になし。 |
| 6. ファイル構成（特定ファイルの指摘） | ⚠️ 要注意 | 直下の画像ファイルの中に、`manga.jpg.png` という二重拡張子となっているファイルがあります。また、`gazo2.png` (約3.2MB) や `seisaku.png` (約2.1MB) など、容量が非常に大きい画像ファイルが存在します。 | ・二重拡張子は混乱を招きやすいため、可能であればリネームしてHTML内の参照も修正することを推奨します。<br>・表示速度（SEO・表示パフォーマンス）向上のため、画像を圧縮するか、WebP形式などの軽量フォーマットへ変換することを推奨します。 |

---

## 追加報告（報告者：Claude Sonnet 4.6）

- 報告者：Claude Sonnet 4.6（Claude Code）
- 報告日時：2026年05月29日
- 対象フォルダ：`C:\Users\mukaihata\Dropbox\mukaihata\研究\webサイト\webサイト_研究\my-website`
- 調査方針：読み取りのみ。ファイルの変更・削除・移動は未実施。

### 調査結果

#### 1. 機密情報のチェック

| 状態 | 内容 |
|---|---|
| ✅ 問題なし | 対象拡張子（.html .js .json .env .md .txt .py .ts）にAPIキー・パスワード・トークン・シークレットの記述なし。実在したファイルは `index.html` と `.claude/launch.json` のみ。 |

#### 2. .gitフォルダの状態チェック

| 項目 | 状態 | 内容 |
|---|---|---|
| .gitフォルダの存在 | ✅ 正常 | `.git/` が存在（非表示属性） |
| ロックファイル | ✅ 問題なし | `index.lock` なし |
| .gitignore | ⚠️ 要注意 | **存在しない** — `.env` 等の機密ファイルが誤コミットされるリスクあり |

**推奨対応：** `.env`、`*.key`、`*.pem` 等を除外する `.gitignore` の作成を推奨。

#### 3. Dropbox同期対象のチェック

| 状態 | 内容 |
|---|---|
| ❌ 問題あり | パスに `Dropbox` が含まれており同期対象。`.git/` フォルダも Dropbox 同期内に存在。複数デバイス間でのgit内部ファイル競合（リポジトリ破損）リスクあり。 |

**推奨対応：** Dropbox の同期除外設定で `.git` フォルダを除外するか、プロジェクト全体を Dropbox 外へ移動することを推奨。

#### 4. 公開リスクのチェック

| 項目 | 状態 | 内容 |
|---|---|---|
| リモートリポジトリ | ⚠️ 要注意 | `https://T-mukkun@github.com/T-mukkun/my-website.git` — Public/Private はローカルから判定不可。GitHub上で要確認。 |
| 依存ファイル | ✅ 問題なし | `package.json`・`composer.json` 等なし |

**推奨対応：** GitHub上でリポジトリの公開設定を確認。Public の場合、メールアドレス（`.git/config` 内の `tsu-mukaihata@hyo-med.ac.jp`）がコミット履歴から閲覧可能な点に注意。

#### 5. 認証・アクセス管理のチェック

| 項目 | 状態 | 内容 |
|---|---|---|
| 接続方式 | ⚠️ 要注意 | HTTPS接続。URLにユーザー名 `T-mukkun` が埋め込まれている。 |
| .netrc | ✅ 問題なし | `~/.netrc` なし。平文認証情報ファイルなし。 |
| credential.helper | ✅ 問題なし | グローバル設定で `manager`（Windows Credential Manager）を使用。平文保存なし。 |

**推奨対応：** SSH接続（`git@github.com:T-mukkun/my-website.git`）への切り替えを推奨。

#### 6. ファイル構成の概要

| ファイル | 備考 |
|---|---|
| `index.html` | メインページ（約39KB）、機密情報なし |
| `manga.jpg.png` | ⚠️ 二重拡張子（`.jpg.png`）— 意図的でなければリネーム推奨 |
| `seisaku.png` | 前回記録にないファイル（約2.1MB）。新規追加と思われる |
| `gazo2.png` | 約3.2MB — パフォーマンス観点で圧縮を推奨 |
| `.claude/launch.json` | ローカル開発用サーバー設定（Python http.server）、機密情報なし |

### 総合サマリー

| リスク | 優先度 |
|---|---|
| `.git/` が Dropbox 内にある（リポジトリ破損リスク） | **高** |
| `.gitignore` がない（機密ファイル誤コミットリスク） | 中 |
| GitHubリポジトリの公開設定未確認 | 要確認 |
| HTTPS接続・ユーザー名URLへの埋め込み | 低〜中 |

---

## 対応リスト（3報告統合・2026年05月29日作成）

### 🔴 高優先度

1. **`.git/` フォルダを Dropbox 同期対象から除外する**
   - Dropbox の設定で `.git` フォルダを同期除外にするか、プロジェクト全体を Dropbox 外に移動する
   - 放置するとリポジトリ破損リスクあり（全報告で一致）

2. **GitHub リポジトリの公開設定を確認する**
   - `T-mukkun/my-website` が Public か Private か、GitHub 上で確認する
   - Public の場合、コミット履歴にメールアドレス `tsu-mukaihata@hyo-med.ac.jp` が含まれていることを認識しておく

### 🟡 中優先度

3. **`.gitignore` を作成する**
   - `.env`、`*.key`、`*.pem` 等を除外対象に設定する
   - 現状は機密ファイルが誤コミットされても防止する仕組みがない

4. **Git 接続を HTTPS から SSH に切り替える**
   - `git@github.com:T-mukkun/my-website.git` 形式に変更する
   - 現状の HTTPS + ユーザー名埋め込みより安全

### 🟢 低優先度（任意）

5. **`manga.jpg.png` の二重拡張子を修正する**
   - ファイル名を `.png` または `.jpg` に統一し、`index.html` 内の参照も合わせて修正する

6. **大容量画像を圧縮する**
   - `gazo2.png`（約3.2MB）、`seisaku.png`（約2.1MB）を圧縮または WebP 形式に変換する
   - ページ表示速度の改善につながる

---

## 対応記録（2026年05月29日実施）

| # | 内容 | 状態 | 備考 |
|---|---|---|---|
| 1 | `.git/` を Dropbox 外へ移動 | ✅ 完了 | `C:\Users\mukaihata\Projects\my-website` へ移動 |
| 2 | `my-website` の公開設定確認 | ✅ 完了 | Public のまま継続（研究ポートフォリオとして意図的公開） |
| 2' | `match-rush` を Private に変更 | ✅ 完了 | 個人メールアドレス露出リスク解消。後日 T-mukkun アカウントで再公開予定 |
| 3 | `.gitignore` を作成・コミット | ✅ 完了 | `.env`、`*.key`、`*.pem` 等を除外対象に設定 |
| 4 | SSH接続への切り替え | 🔜 後回し | 現状 HTTPS + Windows Credential Manager で実害は低い |
| 5 | `manga.jpg.png` 二重拡張子の修正 | 🔜 後回し | |
| 6 | 大容量画像の圧縮 | 🔜 後回し | `gazo2.png`（3.2MB）、`seisaku.png`（2.1MB） |
